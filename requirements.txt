# AverVOX — LLM Speech Bridge
# Add voice to any LLM using an OpenAI-compatible endpoint.
# Python dependencies (install into venv via install.sh)
#
# System packages (installed via apt):
#   python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-appindicator3-0.1
#   libgirepository1.0-dev gir1.2-glib-2.0
#   portaudio19-dev libportaudio2
#   xdotool xclip

# Audio capture + VAD
sounddevice>=0.4.6
webrtcvad>=2.0.10

# STT (local, CPU)
faster-whisper>=1.0.0

# TTS engine — installed by install.sh:
#   piper-tts>=1.2.0

# LLM API client (for Converse mode)
httpx>=0.27

# Global hotkeys
pynput>=1.7.6

# Config
PyYAML>=6.0

# Numeric
numpy>=1.24.0

# Testing
pytest>=7.0
pytest-mock
